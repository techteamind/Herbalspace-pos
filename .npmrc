# bluetooth-serial@1.0.0 mendeklarasikan peer @capacitor/core@^4, padahal proyek pakai
# Capacitor 8 (plugin tetap kompatibel — sudah diverifikasi build APK). Tanpa ini,
# `npm install` di Vercel gagal ERESOLVE → deploy gagal. legacy-peer-deps menyamai
# cara instal lokal.
legacy-peer-deps=true
