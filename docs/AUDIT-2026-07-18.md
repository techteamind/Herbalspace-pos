# Audit Menyeluruh Herbaspace POS — 18 Juli 2026

Hasil audit multi-agent (kode) + audit live di emulator (UI/UX). Dokumen ini adalah
checklist kerja: centang saat selesai, tulis hash commit di sampingnya.
Detail mentah 92 temuan: lihat riwayat sesi audit.

**Status legend:** `[ ]` belum · `[x]` selesai · `[~]` sebagian/di-skip dengan alasan

---

## Fase P0 — Aplikasi tidak bisa menjual (KRITIS, kerjakan duluan)

- [x] **create_sale rusak di produksi**: DB terpasang versi 7-param, API kirim 8 param
      → semua penjualan 500. Fix: advisory lock per-tenant menggantikan `FOR UPDATE`
      invalid, deploy ulang ke DB produksi, overload lama 7-param di-drop.
      _Terbukti live di emulator._
- [x] **Validasi minimal di create_sale**: tolak qty ≤ 0, unit_price < 0, total < 0.
- [x] **Error 500 bocorkan SQL + parameter ke UI kasir** — `handler.ts` kini kirim
      pesan generik (RAISE EXCEPTION bisnis tetap diteruskan sbg 400).
      ⚠ Berlaku setelah deploy Vercel berikutnya.
- [x] **Verifikasi live**: penjualan tunai TRX-20260718-0001 sukses end-to-end;
      void juga diuji dan berhasil; data test dibersihkan.
- [x] **(Temuan baru saat verifikasi) Update APK tidak pernah termuat**: service
      worker workbox mem-precache bundle lama dan terus menyajikannya setelah APK
      di-update (teramati: webview memuat `index-Ml0GFWkj.js` yang sudah tidak ada
      di APK; update tertinggal 1 generasi per restart lalu macet). Fix: SW
      dinonaktifkan + di-unregister di platform native (`src/main.tsx`), registrasi
      manual hanya untuk web/PWA (`vite.config.ts` `injectRegister: null`).

## Fase P1 — Integritas uang & stok (minggu ini)

- [~] Server percaya `unit_price`/`discount`/`taxPercent` klien — SUDAH: validasi
      qty/harga ≥ 0, clamp discount ≤ subtotal, clamp taxPercent 0–100, cek customer
      milik tenant. BELUM: verifikasi unit_price terhadap tabel products/variants
      (butuh payload varian+modifier dari klien — perubahan protokol).
- [x] Idempotency POST /sales: kolom `client_ref` + unique index (sudah dimigrasi ke
      DB produksi), `create_sale` replay-safe, klien kirim uuid stabil per checkout.
      ⚠ Aktif penuh setelah deploy Vercel (API meneruskan clientRef).
- [ ] Antrean offline yang ditolak permanen dibuang diam-diam (`src/lib/offline-sync.ts:47`)
      — simpan ke "gagal" + tampilkan ke kasir.
- [x] Status `paid` dipasang tanpa cek pembayaran — `create_sale` kini menolak
      pembayaran < total ("Pembayaran kurang dari total transaksi"). Sudah dideploy.
- [x] Promo buy-X-get-Y: divisor `buyQty+getQty`, agregasi seluruh baris produk,
      gratiskan unit termurah (harga varian dihitung).
- [x] Promo tidak lagi auto-stack — hanya diskon terbesar yang dipakai
      (`ponytail:` ubah jika bisnis butuh stacking).
- [x] Happy hour lintas tengah malam (21:00–01:00) kini aktif.
- [x] Void race: gate atomik `UPDATE ... WHERE status <> 'void' RETURNING`; restore
      stok kini dari `stock_movements` yang tercatat saat jual, bukan resep saat ini.
- [x] Poin pelanggan kini di-update DI DALAM `create_sale` (atomik). Update terpisah
      di sales.ts dihapus. Sudah dideploy ke DB.
- [~] Laporan terpotong: cap dinaikkan 200→1000 (transaksi) & 100→1000 (expenses),
      klien minta 1000. Fix penuh = agregasi di server (belum).
- [ ] Re-save produk ber-varian meng-orphan optionIds → varian POS rusak
      (`api/_handlers/variants.ts:64`)

## Fase P2 — Keamanan, konsistensi, UX

### Keamanan / scoping
- [~] `?outletId` di GET transactions: kasir kini terkunci ke outletnya sendiri
      (hanya manager/owner boleh memilih outlet). BELUM: validasi `X-Outlet-Id`
      milik tenant di `api/_lib/auth.ts:113`.
- [x] `customerId` lintas-tenant ditolak di POST /sales (cek tenant sebelum create_sale).
- [x] recipes PUT & product-modifiers POST kini `requireRole(manager)`.
- [x] Logout (AuthContext.logout) kini membersihkan query cache + offline queue +
      outlet aktif; kedua tombol "Keluar" dialihkan dari `supabase.auth.signOut()`
      langsung ke `logout()`.
- [ ] Gagal buat karyawan → orphan Supabase user → auto-provision tenant baru sbg owner
      (`api/_handlers/employees.ts:55`)
- [ ] employees PUT bisa demote/nonaktifkan owner terakhir (`api/_handlers/employees.ts:92`)

### Laporan & waktu
- [x] Batas hari dashboard kini WIB (UTC+7): stats hari-ini/kemarin, grafik 7 hari,
      label hari, top-products. ⚠ Aktif setelah deploy Vercel.
- [x] Window "Bulanan" kini 30 hari flat (setMonth overflow di tgl 29–31 hilang).
- [ ] Shift "Semua Outlet" double-count; null-outlet shift menjumlah seluruh tenant;
      dua shift aktif race; kasir bisa tutup shift siapa pun (`api/_handlers/shifts.ts:38,60,78`)

### Offline & sesi
- [ ] Cache memory-only → offline mati setelah restart app (`src/lib/query-client.ts:14`)
- [ ] Replay antrean pakai outlet/token saat sync, bukan saat capture
      (`src/lib/api-client.ts:34`, `src/lib/offline-sync.ts:33`)
- [ ] Queue hanya aktif saat `navigator.onLine === false` — jaringan flaky kehilangan
      transaksi (`src/lib/api-client.ts:29`)
- [ ] Sukses offline tampil nomor struk `undefined` + tanpa indikasi "menunggu sync"
      (`src/features/pos/payment-sheet.tsx:123,143`)
- [ ] `/me` gagal → role null sampai 1 jam tanpa retry (`src/contexts/AuthContext.tsx:61`)

### Struk & print
- [ ] Link struk publik selalu 401; setelah diperbaiki, response bocorkan PII + COGS;
      struk void masih bisa dishare (`api/_handlers/share-receipt.ts:31,41,57`)
- [ ] Link share WA = `https://localhost/...` di APK (`src/features/pos/index.tsx:495`)
- [ ] Print & share error di-swallow `catch {}` kosong (`src/features/pos/index.tsx:486,499`)
- [ ] Thermal: UTF-8 mojibake non-ASCII; serial port basi setelah disconnect
      (`src/lib/thermal-printer.ts:5,109`)
- [ ] Catatan item cart tidak pernah dipersist (`payment-sheet.tsx:135`)
- [ ] Service charge setting tidak pernah diterapkan (`payment-sheet.tsx:102`)
- [ ] Pembulatan pajak klien (bulat) ≠ SQL (2 desimal) (`payment-sheet.tsx:102`)

### Inventori
- [ ] Stock adjust read-modify-write non-atomik (`api/_handlers/stock-movements.ts:30`)
- [ ] CSV import parse "10.000" jadi 10 (`src/features/products/index.tsx:53`)
- [ ] Ganti unit bahan tanpa konversi stok/resep (`src/features/inventory/ingredient-form.tsx:22`)
- [ ] Makna "return" tidak konsisten (keluar vs masuk) (`src/features/stock-movements/index.tsx:12`)
- [ ] Clear SKU/kategori/foto di edit produk tidak tersimpan (`src/features/products/product-form.tsx:91`)
- [ ] Phantom price-history rows tiap edit produk (`api/_handlers/products.ts:57`)

### UI/UX (temuan live emulator)
- [x] Layar Kasir kini punya empty-state ("Belum ada produk" / "Tidak ada yang cocok").
- [~] Hapus produk: mutation SUDAH invalidate `["products"]` — yang terlihat saat
      audit kemungkinan latensi refetch (cold start Vercel) tanpa feedback. Tambah
      pending/optimistic state jika masih terasa.
- [ ] Back hardware keluar app alih-alih tutup sheet pembayaran
- [ ] Sheet pembayaran tetap terbuka saat cart kosong, "uang diterima" basi
- [ ] Tab aktif "Laba Rugi" putih di atas hijau muda — kontras hampir nol
- [ ] Konten Laporan terpotong di belakang bottom-nav (kurang padding bawah)
- [ ] "struk hari..." terpotong di kartu dashboard; header duplikasi nama outlet
- [ ] Cold start "Halo, Kasir" sebelum profil termuat
- [ ] Input "Uang diterima" tanpa format ribuan
- [ ] Copy warisan kedai kopi ("Kopi Susu Gula Aren", "less sugar, extra shot")
- [ ] Dialog hapus bilang permanen padahal soft-delete
- [ ] Void gagal diam-diam; simpan Settings/Kategori/Promo/dll gagal diam-diam
      (`src/features/transactions/detail.tsx:69`, `src/features/settings/index.tsx:98`)
- [x] Duplikat HP pelanggan di checkout kini mengembalikan pelanggan yang sudah ada
      (200), bukan 500 mentah.
- [ ] Filter outlet riwayat transaksi tidak bisa di-deselect (`src/features/transactions/index.tsx:32`)
- [ ] Back arrow halaman Shift mati saat loading (shadowed `history`) (`src/features/shifts/index.tsx:15`)
- [ ] Audit log sale mencatat entity id `undefined` (`api/_handlers/sales.ts:56`)
- [ ] OutletPicker tidak invalidate cache; owner cold start scope salah
      (`src/features/outlets/outlet-picker.tsx:59`)

### Terbantah (jangan dikerjakan)
- ~~Retry gagal-buat-pelanggan menduplikasi customer~~ — ditahan index unik
  `customers_tenant_phone_unq`; yang perlu diperbaiki hanya pesan errornya (lihat P2 UX).

---

## Catatan proses audit
- 10 agent domain paralel + verifikasi adversarial (21/22 confirmed real, 1 refuted).
- 1 agent (keamanan API) diblokir safety filter; area authz dicakup agent lain.
- Emulator: AVD `ecofrag_pixel`, APK debug build dari commit be32d14, backend produksi.
- Data test sudah dibersihkan (produk TEST dihapus; tidak ada transaksi tercipta).
